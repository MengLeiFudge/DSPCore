namespace DSPCore;

/// <summary>
/// 定义旧 BuildBarTool 兼容层级。
/// Defines legacy BuildBarTool-compatible build bar tiers.
/// </summary>
public enum BuildBarTier
{
    /// <summary>
    /// 原版主行。
    /// Vanilla primary row.
    /// </summary>
    Primary = 1,

    /// <summary>
    /// 扩展第二行。
    /// Extended second row.
    /// </summary>
    Secondary = 2
}
