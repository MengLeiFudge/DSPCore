# Building Parameters Example

Use this when a building has settings that should survive copy-paste and blueprint placement. Keep the block ID stable and small.

Use the `Blueprints.Register(...)` integer-payload overload for ordinary settings. Use `BuildingParameterDescriptor` only when you need full `BuildingParameterBlock` control.
