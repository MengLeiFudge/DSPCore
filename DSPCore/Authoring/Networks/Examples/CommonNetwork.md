# Common Network Example

Use this when a feature needs to ask whether two entities share a mod-defined network.

Use the `Networks.Register(...)` parameter overload for ordinary adapters. Use `NetworkDescriptor` only when code needs to pass descriptor objects around directly.
